<UserControl x:Class="Euchre.UserControls.CardDisplayUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Euchre.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="138" d:DesignWidth="835">

    <UserControl.Resources>
        <Style TargetType="ToggleButton">
            <Style.Setters>
                <Setter Property="Width" Value="100" />
                <Setter Property="Height" Value="136" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Background" Value="Transparent" />
            </Style.Setters>

            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Background" Value="Transparent" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <!-- The control to show all the cards in the player's hand. -->

    <ItemsControl ItemsSource="{Binding CardImages}" 
                    HorizontalAlignment="Center" HorizontalContentAlignment="Center">

        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel Orientation="Vertical"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>

        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <StackPanel>

                    <!-- ToggleButton with card image -->

                    <ToggleButton Margin="{Binding CardMargin}" Checked="ToggleButton_Checked" 
                                    IsEnabled="{Binding IsEnabled}" Visibility="{Binding CardVisibility}" 
                                    Tag="{Binding Card}">
                        <Image Source="{Binding ImageData.Source}" Stretch="Fill" />
                    </ToggleButton>

                    <!-- Card image displayed when disabled -->

                    <Image Source="{Binding ImageData.Source}" Stretch="Fill"
                            Visibility="{Binding ImageVisibility}" Margin="{Binding CardMargin}"
                            Width="100" Height="136"/>
                </StackPanel>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
